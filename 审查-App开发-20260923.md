# 审查报告 · 技术可信度轴 ｜ @App开发 ｜ 2026-09-23

**审查对象**：`finance-app-demo/金融客户定制App产品方案-演示版.pptx`
**只审不改**：pptx、`build-ppt.py`、`app.js`、`styles.css`、`index.html` 一律未改动；未动 git；未弹 PowerPoint；未 pip 安装任何依赖。
**本报告新增文件**：仅本文件（leader 在任务里授权的产出路径）。

**审计基线（供后续修复轮校验备份完整性）**
`金融客户定制App产品方案-演示版.pptx` ｜ **sha256 `75ae16e3ba2bf3542b00d52c0cd3a636591b832f3ab88c4a3f924a961606a023`** ｜ mtime `2026-09-23 14:33:03` ｜ size `1604999` ｜ **15 页 / 218 media**（审计开始与结束两次测量逐项一致 ⇒ 审计全程未触碰产物）。
**重建命令（由 leader 实测、本报告已独立复核）**：`uv run --no-project --with python-pptx --with pillow python finance-app-demo/build-ppt.py`（py 3.12.5 / python-pptx 1.0.2 / Pillow 12.3.0；首次需联网，`--offline` 解析不出来）。

**⚠️ 审计期间的外来改动（与本次审查无关，但会污染"源文件未动"这句话）**
- `finance-app-demo/app.js` 在 **14:42:14** 被**本审查之外的某方**改动：`23292 → 23316` 字节（**+24**），mtime 由 `14:31:19` 变为 `14:42:14`。**本审查从未写入该文件**（全程只读：read/grep/wc/cat），`app.js` 所在目录在 git 里是 **untracked**（`?? finance-app-demo/`），故**无 diff 可查、无法回溯内容差异**。
- **影响评估（已重验）**：审计对象 **pptx 未受影响**（sha256/mtime/15 页 218 media 全部未变）⇒ 本报告对 pptx 的全部结论**继续有效**。§4.4 对 demo 的取证**逐条重验后仍成立**：`app.js` 仍是 **115 行**、行情三指数（`3,885.33/13,384.56/3,285.58`）与「1,286 人正在观看」**仍是硬编码**、`fetch`/`XMLHttpRequest`/`Notification`/`serviceWorker`/`localStorage`/`WebSocket` **仍为 0 命中**、也**没有**新增任何"示意/原型/免责"文案。
- **⇒ 结论措辞修正**：应说"**pptx 与 `build-ppt.py` / `styles.css` / `index.html` 未动；`app.js` 被审查之外的改动动过 +24 字节，且不影响本文任何结论**"，而不是笼统的"四个源文件 0 字节改动"。

**方法（全部可复现，只用 python3 标准库，不需要 python-pptx/PIL）**
- 用 `zipfile` + `ElementTree` 直接读 pptx 的 OOXML：页数、每页图片摆放坐标、`<a:t>` 计数、`docProps`。
- 自己写了一个 ~40 行的 PNG 解码器（zlib + 逐行反滤波）读 `ppt/media/*.png` 的 **alpha 通道**，据此算每张文字位图的**墨迹包围盒与逐行墨迹分布**——用来做**不依赖读图**的裁切/越界/互压检测。
- 用 `grep` 核 demo 的真实能力面（网络 API、原生 API 调用）。

> **一、我读不了图。** `read_image` 实测报错：`model "deepseek-v4.1-flash" does not declare image input`。
> ⇒ **封面与版式的"视觉判断"我没做，也不猜**，已按 leader 指示把该轴交回 @产品经理（他在 Thread 里主动认领过）。
> 我改为用上面的机器方法做**可量化的版式核验**（裁切/越界/重叠/贴边），这部分是硬证据，不是观感。

---

## 〇、先更正一条会改结论的前提：这份 pptx 已被重新生成

| 项 | leader 结稿时的事实 | 我实测（14:33 起） |
|---|---|---|
| 页数 | 14 页 | **15 页** |
| `ppt/media/` | 200 张 | **218 张** |
| slide 内 `<a:t>` | 0 | **0**（成立） |
| 文件 mtime | 10:57:28 | **14:33:03**（zip 内条目 14:33:02） |
| 与源码先后 | 产物比源码旧 | 源码 14:31:19 → **产物晚 1 分 44 秒** |

**⇒ 「产物比源码旧」已不成立。** 现在这份是**新产物**，且我正面验完它**结构上就是当前 `build-ppt.py` 的忠实产物**（见第一节）。
**⇒ 但 leader 的另两条事实（无文字层、依赖缺失）成立**，见第一、五节。

---

## 一、产物的可复核基线

**1.1 结构核验：产物 = 当前源码的产物（置信度高）**

- pptx **15 页** ↔ `build-ppt.py` **15 处 `new_slide()`**（232 / 242 / 258 / 280 / 297 / 310 / 325 / 342 / 351 / 369 / 383 / 399 / 415 / 438 / 467 行）。
- 逐页图片摆放数 `{1:8, 2:16, 3:19, 4:15, 5:15, 6:19, 7:19, 8:16, 9:19, 10:16, 11:20, 12:21, 13:20, 14:21, 15:15}`，与源码每页的 `text()` 调用数 + `add_phone()` + 图标数**逐页吻合**。
- **259 处图片摆放** = **233 处文字摆放** + **26 处非文字摆放**；按媒体文件去重后 = **218 张** = **204 张文字位图** + **14 张非文字图**（2 张 1600×900 背景/封面图 + 8 张 430×900 截图 + 4 个 180×180 图标）。
  ⇒ **"233" 是摆放次数（= `text()` 调用数），"204" 才是不同位图张数**（去重 29：眉题、页脚、`↓`/`→` 等重复串共享同一张 PNG）。见第六节更正记录。
- **每一处文字摆放**的位图像素尺寸**全部**等于其摆放框的 `int(w×144) × int(h×144)`（`TEXT_SCALE=2`，19/39-40 行）——这是 `text()` 独有的特征，截图/背景/图标都不满足。

**1.2 无文字层（成立，且比 leader 说的更精确）**

- `ppt/slides/*.xml` 中 `<a:t>` = **0**。
- 全包 86 个 `<a:t>` **全部位于 `ppt/slideLayouts/*.xml` 与 `slideMaster1.xml`**（python-pptx 默认模板残留），与内容无关。
- **`ppt/notesSlides/` 不存在、批注不存在**（rels 引用 0）⇒ **全篇没有备注页**：客户/销售自己翻这份稿时，拿不到任何讲解脚本或口径提示。

**1.3 「不可编辑/不可搜索/不可复制」的取舍，我的判断**

- 技术事实：客户拿到的 15 页**没有一个可编辑字符**，改一个字必须重跑脚本。
- **我的判断（与 leader 给的选项对齐）：作为「对外方案稿」不可接受。** 理由不是技术洁癖，而是销售场景的真实动作：改价、删竞品名、调整口径、按客户改一页——这些都是**销售每天在做的事**，而现在每一项都要走开发 + 一次重建。
- **建议改法（不改本稿也能立刻缓解）**：① 保留 `build-ppt.py` 作为**生成源**；② 对外再出一版**可编辑稿**（把 `text()` 换成真正的 pptx 文本框，`TEXT_FONT` 已指定 `STHeiti Medium`，配 `font.name`/`ea` 即可）；③ 至少配一份 Markdown **讲稿/口径页**给销售。

---

## 二、版式层机器核验（不依赖读图）

**2.1 文字裁切 —— 1 处真裁切（高，可见破版）**

| 项 | 值 |
|---|---|
| 页 | **第 8 页**（`07 / PRIVATE CIRCLE`，`build-ppt.py:341-348`）|
| 行号 | `build-ppt.py:346`：`text(slide, "圈子不是简单群聊，而是结构化、可持续运营的内容社区。", 6.9, 1.5, 5.3, 0.35, 16, NAVY, True)` |
| 证据 | 位图 `image100.png` = **763×50 px**（= 5.3in × 0.35in × 144）。逐行墨迹分布：**第 1 行占 row 2–31，row 32–36 空，第 2 行从 row 37 起、到 row 49（画布最后一行）被硬切**——26 个汉字在 763px 内必然折行，2 行需要 ≈70px，画布只有 50px。 |
| 客户看到什么 | **第二行只剩上半截**（末尾约 3 个字被横切）|
| 改法 | 该框高 `0.35` → **`0.6`**（见下方取值推导）。同页其他框无此问题。 |

**为什么推荐 `0.6` 而不是 `0.75`（@产品经理 的约束 + 我的实测推出）**
- 2 行需要 ≈**68px**（行高 36px：第 1 行墨迹 row 2–31、第 2 行从 row 37 起、墨迹底 ≈67–68）。
- `h=0.6` → 位图 **86px**，余量 **18px**，够装且不紧；框下沿 **1.5+0.6 = 2.10in < 2.15in**（第一条 bullet 的标签框起点）⇒ **连"摆框相叠"都不会产生**。
- `h=0.75` 虽然内容也放得下，但框下沿 2.25in 会与第一条 bullet 框**相叠 0.10in**——**墨迹其实不撞**（导语墨迹底 ≈1.97in < 2.15in），可**摆框派复核会凭空多报一条"文字互压"**。若坚持用 0.75，复核必须继续用**墨迹包围盒**判互压（见 2.6）。
- **另注意**：改框高会让这张位图从 50px 变 86px，**复核时要按新画布高判"是否触底"，不能拿今天的 row 49 当期望值**。

**2.2 全片无越界（通过）**：所有图片/形状都在 13.333×7.5in 画布内，无出血、无出界。

**2.3 无空位图（通过）**：**204 张文字位图（对应全部 233 处摆放）无一为空白**——不存在"文案渲染成空图导致整句消失"的情况。

**2.4 页码位图右缘（偏好，经复核后降级，不建议返工）**
11 页的页码（`12.25, 0.55, 0.45×0.25`，位图 64px 宽）墨迹到达**最后一列 63/64**。
**降级理由（由 @产品经理 提出、我复核接受）**：`text()` 在 `align=RIGHT` 时 `left = px_w - line_width`（`build-ppt.py:68`），墨迹**本就应该止于位图右缘**——这是右对齐的正常结果，不能据此判定"被切"；且该位图距版面右缘还有 0.63in，与文字裁切不是一类问题。
⇒ 记为**偏好**：即便真有 1px 差，也在 20px 字形的 5% 以内，**不值得为此重跑一版**。

**2.5 封面页脚重复绘制（低）**：第 1 页**同一坐标** `(0.70, 7.13, 2.00×0.18)` 有两张「小鹅通·金融BU」位图（`image1.png` + `image8.png`）——`new_slide()` 内建的页脚（`build-ppt.py:223`，色 `RGBColor(184,201,229)`）与 `build-ppt.py:239` 的**二次绘制**（色 `RGBColor(210,226,250)`）叠在一起，两次抗锯齿不同色 ⇒ 封面页脚比其他页更"糊/更重"。改法：**删掉 239 行**。

**2.6 文字互压（通过，仅 2 处包围盒擦边已排除）**：逐对比较**墨迹包围盒**（不是摆框）：第 9 页 `(0.85,5.45)` 与 `(8.85,5.65)`、第 14 页 `(0.70,0.82)` 与 `(0.85,1.35)` 摆框有 0.02–0.15in 交叠，但**实际墨迹不相交**（长句墨迹右界远在左侧）⇒ **不构成缺陷**。

---

## 三、源码级问题（不需读图即可复核）

**3.1 13 个 kicker 是死代码（中）**

`title()` 定义在 `build-ppt.py:87-90`，签名收下 `kicker` 参数却**从不使用**——页眉只画死了「金融行业自主私域 App 方案」。
因此 `01 / PLATFORM DEPENDENCE` … `13 / COMPLIANCE & SECURITY` 这 13 串**一页都不会出现**（调用点：243 / 259 / 281 / 298 / 311 / 326 / 343 / 352 / 370 / 384 / 400 / 416 / 439 行）。
- 后果：**客户看到的是 13 页重复同一句眉题**，作者写好的分节标签全丢。
- 附带 bug：kicker 编号与 `page` 编号**系统性差 1**（第 2 页 kicker 写 `01`、页码印 `02`）——若要启用 kicker，必须同时改编号。
- 改法：`title()` 里用 kicker 替换写死眉题（并统一编号），**或**删掉这 13 个实参。

**3.2 封面有两个"死函数"（中）**

`grep -n` 结果：`add_cover_device`（105 行）、`add_cover_gradient`（150 行）**各自只有 def 行、全文件 0 个调用点**；封面实际只调了 `add_cover_art()`（166 行定义 → **233 行调用**）。
⇒ **封面没有手机样机图**；`screenshots/01-home-platform.png`（1440×960，全套截图里唯一一张"首页"）**在稿子里零使用**。两个未被采用的封面方案（设备图版、纯渐变版）说明作者迭代过封面，**迭代痕迹留在了源码里**。
改法：在 233 行后补 `add_cover_device(slide, x, y)`（这是最直接提升封面视觉的一步——见第六节"版式/读图轴"）；若是有意为之，**删掉这两个函数与那张图**，避免后人再踩。

**3.3 文档属性是 python-pptx 模板残留（低）**

`docProps/core.xml`：`dcterms:created = 2013-01-27`、`lastModifiedBy = Steve Canny`、`dc:description = generated using python-pptx`；`docProps/app.xml` 还写着 `PresentationFormat = On-screen Show (4:3)`（实际是 13.333×7.5 = 16:9）、`Application = Microsoft Macintosh PowerPoint`。
⇒ 客户若看「文件 → 属性」，看到的是**2013 年**和别人的名字，与稿子外观不符。改法：`prs.core_properties` 里补 `title/author/created`。

**3.4 正文最小字号（偏好）**：眉题 9pt、多条正文 10pt、页脚 8pt；投影场景偏小，但不至于不可读。**纯偏好，不建议因此返工。**

**3.5 `text()` 有两个"假开关"：`bold` 与 `font` 全是死参数（中）**

`build-ppt.py:37` 的签名收下 `bold=False` 与 `font="STSong"`，但函数体（38-75 行）**从头到尾没有读过这两个变量**：字体写死用 `TEXT_FONT = "/System/Library/Fonts/STHeiti Medium.ttc"`（18 行，43 行调用），字形粗细只有字号。
- **`font` 死参数**：由 @产品经理 发现。全篇实为 **STHeiti**，作者若本意标题用宋体，意图没落地。
- **`bold` 死参数（本报告补充）**：**作者传的是位置实参、不是关键字实参**，例如 `build-ppt.py:89` 的 `text(slide, heading, 0.7, 0.82, 8.5, 0.55, 26, NAVY, True)` —— 那个 `True` 落在第 9 位、正是 `bold`。因此 **`grep -c 'bold=True'` = 0 并不能证明"没人传 bold"**，这是本条 finding 最容易被人反手推翻的地方（由 @产品经理 指出，我复核确认）。真实情况是：`title()` 的页标题（89 行）、`bullet()` 的标签（213 行）以及正文里大量强调串**都以位置实参传了 `True`，全部无效** ⇒ 全稿**字重层级完全没有渲染**，视觉层级只靠**字号 + 颜色**撑着。
- 影响：① 与 kicker、`add_cover_device`、`add_cover_gradient` 同属"**看着像配置、其实没作用**"的假开关，**下一个人改稿时会被骗**（把字号调大却期待变粗）；② 若之后有人把 `bold` 接上真字体字面，**全稿折行位置必然重排**（字宽变了），并因此**可能新增裁切**，属**必须预留复核的连锁风险**，要在改之前知道。**注意区分**：重排是**必然**，新增裁切是**可能**。
- **改法（本轮已定案，2026-09-23）：分两种写法，绝不一起删**
  - **`font="STSong"`：删签名那一行即可，0 处调用受影响**（全文件 `font=` 关键字 0 次、第 11 位位置传参 0 次）。
  - **`bold`：本轮不动调用点**，把形参改名为 `_bold_unused`（或加注释"未实现，勿传"）并**保留其位置**。理由见下。
  - **要"接通字重"是独立一轮**：`TEXT_FONT`(`:18`) 是**单一字面、无 `index=`** ⇒ 得换字面（ttc 内 `index=` 或换 Bold 文件），**折行必然重排 → 必须重跑全稿裁切核验**。
- **⚠️ 为什么不能直接删 `bold`（本轮最贵的一个坑，`ast` 静态扫描实测）**：`text()` 静态调用点 **89 处**，其中 **71 处在第 9 位位置实参上传了 `bold`**，**25 处**还接着传了第 10 位 `align`（精确行号：`90, 272, 273, 274, 275, 276, 334, 338, 390, 391, 404, 405, 407, 429, 432, 433, 434, 435, 456, 458, 459, 460, 462, 463, 464`）。全文件 **`bold=` 关键字 0 次、`font=` 关键字 0 次**（即所有调用都是纯位置传参）。
  删掉 `bold` 后签名变为 `(…, align, font)` ⇒ 旧第 9 位 `True` 落到 `align`、旧第 10 位 `PP_ALIGN.RIGHT` 落到 `font`；**Python 不做类型检查，`align=True` 既不等于 `CENTER` 也不等于 `RIGHT`，直接落进 `:69-70` 的 `else: left = 0`（左对齐）** ⇒ **页码会静默变成左对齐，不抛异常**。这是"删一行"会引发的静默错位，**必须逐处改，不能只改签名**。
  **最直观的受害者是页码**：那 25 处里凡传 `PP_ALIGN.RIGHT` 的（`title()` 的页码，`:90` 等），`left = px_w - line_width` 会退化成 `left = 0`，**页码从 `12.25` 起的框内左缘开始画 ⇒ 肉眼可见跑位**，而且不报错——最容易被误判成"改完变丑了"而不是"改错了"。（@产品经理 指出，我复核源码 `:61-70` 确认。）
  ⇒ 本报告因此推荐**改名而非删除**：零调用点改动，同样消掉"骗下一个改稿人"的效果。
- **可断言的上界**：上述两条路**都不改变渲染**（`bold` 从未被读取）⇒ 修复后**只断言"位图张数 / 尺寸集合不变"**；**不做像素级或字节级断言**——重跑用的是**当前 Pillow**，栅格化与编码未必与生成这份时相同，"只删死代码 ⇒ 像素逐张相同"在换过 Pillow 的前提下不可证。

---

## 四、技术可信度：逐页能力承诺 → 实现对照（本轴核心）

### 4.0 两条判定规则（照 leader 给的纪律）

1. **`品牌APP功能清单.md` 的 ✅ = OpenSpec 规格，规格 ≠ 实现**（该文件第 3-4 行自述：核对方式是"规格全量提取 + 代码结构与配置文件实测"）。
2. 任何「我们有/我们支持」都必须能**指到实现**。指不到 ⇒ finding。

### 4.1 权威口径（我方自己的文件，晚于功能清单 6 天、且是决策人裁定）

- `基础包范围与报价单-收窄版.md:8-15`：**30 万基础包只承诺三块** = 基础课程交付能力 + 品牌配置化壳 + 三端打包上架（鸿蒙实测待定）。
- 同文件 `:20-31`：「**本报价不含**」清单 = **圈子/社区/消息中心、极光推送、合规组件、离线下载、播放器增强、后台配置化装修** —— 一律**「当前不承诺」**，移入定制菜单。
- 同文件 `:4`：「**规格存在 ≠ 代码已实现**。在实测核对完成前，报价单只能承诺已确认能力，否则就是拿交付违约换签约。」
- `app-delivery-notes/交付成本与工期边界.md:45-62`：决策人口径「基础包就是那个 Flutter 工程，**只包含基础课程交付能力，其余能力目前都没有**」；圈子 ❌没有、消息中心/极光推送 ❌没有、合规组件 ❌没有，标注**「不可承诺」**。
- 同文件 `:183`：**不可承诺清单第 1 条**——"不承诺我们没有的行情/交易能力"；`:176` 称该清单**「触之即违约风险」**。
- 同文件 `:246`（功能清单 §十六）：**无原生直播模块**，"直播"由 H5 承载；`:81-89` 行情/诊股/原生直播全属 **L3（占 1 项目位）**，且行情数据授权约 **30 万/年**（`:132`）。
- `基础包范围与报价单-收窄版.md:138`：**待办 #1「提供 Flutter 工程路径 / 实际已实现能力清单」——责任：决策人，阻塞报价准确性**，状态未完成。

> **⇒ 截至本报告，"基础包到底实现了什么"这个前提问题在我方文件里仍是未闭合的待办。**
> 本机 `find . -name pubspec.yaml` **零命中** ⇒ **那个 Flutter 工程确实不在工作区**，无法做代码级核对。这是本次审查的**最大信息缺口**，不在你我能力范围内，需 human/决策人补。

### 4.2 逐页对照

| 页 | 稿中承诺（行号） | 对回现实 | 判定 |
|---|---|---|---|
| 1 封面 | 「圈子运营 · 消息触达 · 投教学习 · 直播服务 · 会员承接」(237) | 5 项里只有**投教学习**（基础课程）在已确认范围内；圈子 ❌、消息 ❌、直播=H5 承载非原生、会员=交易侧未确认 | **高** |
| 2 `01` | 平台依赖叙事 | 叙事页，无能力承诺 | 通过 |
| 3 `02` | 经营风险叙事 | 同上 | 通过 |
| 4 `03` | 影响叙事 | 同上 | 通过 |
| 5 `04` | 「稳定触达：**Push、站内消息和消息中心**形成自有触达链路」(301) | 极光推送 ❌不承诺；消息中心 ❌不承诺（功能清单 §九 有 spec，但决策人口径"没有"） | **blocker** |
| 6 `05` | 竞品路线 **实名 7 家**：财学堂 / 金斗云 / 财源滚滚 / 好人好股 / 点金手 / 大阳智投 / 大决策 (313) | 技术侧无冲突；**对外稿点名竞品的法务/商务风险需 human 判断** | **中（判断项）** |
| 7 `06` | 四层架构含 **服务层：行情·投顾·会员·订单**(327)；链路含 圈子/消息/会员(335) | 行情 ❌无(L3，且数据授权 30 万/年)、投顾 ❌、订单=H5、圈子 ❌、消息 ❌ | **高** |
| 8 `07` | 「圈子：金融私域的核心承载」整页 + 2 张圈子截图 (344-348) | 圈子 ❌不承诺 | **blocker** |
| 9 `08` | 「圈子内的内容与用户互动」整页 (353-366) | 同上 | **blocker** |
| 10 `09` | 「消息推送：持续触达用户」整页 + "Push + 站内消息 + 用户标签"(371-380) | Push/消息中心/用户标签 ❌不承诺 | **blocker** |
| 11 `10` | 用户分层与召回：标签来源 / 沉默唤醒 / 会员到期 (385-395) | 标签、召回、会员状态均无实现可指；会员到期需交易侧 | **高** |
| 12 `11` | 内容/直播/圈子/会员联动 + 4 张截图，**已标「Demo 示意」**(412) | 标注**诚实**（记功）；但会员权益/IAP ❌不承诺 | **中** |
| 13 `12` | 「可配置的品牌 App」(429)、「多端发布 / H5 与原生组合」(433)、「品牌能力：Logo、主题色、**首页 Banner、Tab 和页面风格可定制**」(418)、「后台能力：内容、课程、**圈子、直播、消息和用户标签**统一运营」(419) | 品牌壳（名称/包名/Logo/主题色/启动路由/配置）✅**可承诺**；**首页 Banner/Tab/页面风格 = 后台配置化装修 = 待技术确认项**（混淆）；多端发布 ✅（鸿蒙待实测，稿中未提）；后台那串能力 ❌ | **高** |
| 14 `13` | 「合规组件：R3/C3 适当性、风险提示、执业信息、隐私协议与 SDK 披露」(442)；「安全控制：…租户隔离、敏感操作审计留痕」(443)；「部分私有化部署」(445) | 合规组件 ❌不承诺（收窄版 §二）；R3/C3 上卡片在 `交付成本与工期边界.md:87` 记为 **L2 需定制字段**；租户隔离/审计留痕无实现可指；私有化属 **L3（20–60 人日、占 1 项目位）** 稿中未提成本。**记功**：445 行已写"不等同于全量私有化"、464 行已写"需结合客户主体/数据分类分级/法务安全评审"——这两句是**正确且必要的 hedge** | **高** |
| 15 尾页 | 实施路径 阶段 1「首页/学习/我的/登录/**合规**」、阶段 2「圈子/消息/Push/直播预约」、阶段 3「投研/会员/标签/召回」(470) | 阶段 1 含未确认的合规；阶段 2/3 **整段是未交付能力**，且稿中无「占项目位/前置成本/排期前置条件」任何提示（`交付成本与工期边界.md:142-153` 明确 L3 占 1 个项目位、第 4 单要排队） | **高** |

### 4.3 系统性根因（比任何单页都重要）

**全篇 15 页没有任何一处区分「已交付 / 待技术确认 / 不在范围」。** 一张"能力页"和一张"愿景页"长得完全一样。
- 稿子的**叙事**（平台依赖 → 风险 → 为什么自有 App → 蓝图 → 圈子/消息/运营 → 能力 → 合规/部署 → 实施路径）**本身成立**，我认可它的说服力；
- 但它被拿去对着**收窄版报价单**卖的时候，客户会把**每一页都当成"我付钱就能拿到"**——而按我方自己文件，其中 圈子 / 消息中心 / Push / 合规组件 / 行情 属于**明文"不可承诺"**（`:183` 称触之即违约）。
- **最小改动、最大收益的改法**：加**一层交付状态标注**，不要删愿景。例如统一页脚一行：
  `「本方案含规划与定制能力；实际交付范围以签约的《基础包范围与报价单》为准」`
  并对 圈子/消息/Push/合规 四处文案加「**定制项**」角标（第 5、8、9、10、13、14 页）。

### 4.4 截图的可信度（补充发现）

9 张截图**全部是纯静态 H5 原型**，不是 Flutter 品牌 App 的实拍：
- `finance-app-demo/README.md:3` 自述「**纯静态 H5 交互演示**」，可部署 GitHub Pages；
- `app.js` 仅 **115 行**；`grep` **零命中** `fetch(` / `XMLHttpRequest` / `Notification` / `serviceWorker` / `localStorage` / `WebSocket` ⇒ **无后端、无推送、无持久化**，全部状态在内存里；
- 行情指数 `3,885.33 / 13,384.56 / 3,285.58`（`app.js:58`）与「**1,286 人正在观看**」（`app.js:39`）都是**硬编码字面量**。

⇒ 只有第 12 页标了「Demo 示意」(412)；第 5 / 8 / 10 页把同样的截图与「我们有」文案并置，**没有任何一处说明"截图是原型、不是已交付产品"**。而行情条出现在截图里（`08-market.png`），恰是 L3 + 30 万/年授权的能力——**容易被客户理解为"行情已含"**。

---

## 五、我无法自行结论的点（需 human / 作者补信息）

1. **14:33:03 那次重生成是谁、用什么解释器跑的？** 我实测 `python3`、`python3.12`、`/usr/bin/python3`、`/opt/homebrew/bin/python3`、`~/.venv-html-to-docx` **全部 `ModuleNotFoundError: No module named 'pptx'`**；但 `finance-app-demo/__pycache__/build-ppt.cpython-312.pyc`（10:57）说明**曾有 3.12 解释器成功 import 过它**。这条命令值得留下来，否则改一个字都重建不了。
2. **基础包真实实现清单**（收窄版 §七 待办 #1，责任=决策人，仍阻塞）。没有它，"能不能承诺"只能按最保守口径判。
3. **逐字内容校验**：我验的是**结构与几何 1:1**；**位图里的字符串我读不了**（无图像输入）。建议 @产品经理 用他的 `read_image` **抽查 5–8 张位图**（如 `image11.png` 眉题、`image12.png` 第 2 页大标题、`image181/185.png` 第 13 页、`image100.png` 第 8 页那处裁切）做逐字比对，把"产物=源码"从"结构一致"升到"内容一致"。

---

## 六、复核与更正记录（双人交叉复核的处理）

产品经理 在同 Thread 独立交了内容/口径轴报告，并对本报告提了两条更正。**两条我都重新实测后处理如下**，原报告文字已就地改过，此处留痕。

**6.1 接受：「233 张文字位图」应为「233 处摆放 / 204 张去重位图」，同理「26 处非文字图」应为「26 处摆放 / 14 张去重图」**
- 他的口径：**218 media = 204 文字位图 + 14 非文字**（2 背景 + 8 截图 + 4 图标）。
- 我复算（同一脚本，改按媒体文件去重）：`总摆放数 259`、`distinct media 218`、`distinct 文字位图 204`、`distinct 非文字图 14`，非文字明细逐张列出且与"2 背景 + 8 截图 + 4 图标"完全吻合 ⇒ **他的更正是对的**。
- 我原来的错误是**单位混淆**：把"摆放次数"写成了"位图张数"。**对结论无影响**（裁切/越界/互压都是按摆放逐处算的），但对外引用必须用准确单位，否则会被人拿"你到底数的是什么"推翻整段。

**6.2 接受：页码右缘 1px 不是缺陷，已从「低」降为「偏好」，并明确不返工**
理由见 2.4（右对齐下墨迹止于位图右缘是正常结果；`build-ppt.py:68`）。**我原来那句"最后 1px 被切"缺乏判据**——我无法在不引入字体度量渲染器的前提下区分"右边界正好贴齐"与"真被切 1px"，**证据不足就不该写成缺陷**。这条按他的判断走。

**6.3 我补充了一条他与我原报告都漏掉的：`bold` 死参数**（见 3.5）。他报了 `font` 死参数，我据此复查同一行，发现 **`bold` 也是死参数且影响面更大**——它让全稿字重层级静默失效。**这正是双人交叉复核的价值所在。**

**6.4 他提供、我未覆盖的两处证据（已并入 leader 汇总，不重复劳动）**
- **P6 的 7 家"竞品"实为我方客户/在谈线索**（附 `基础包销售作战一页纸-20260920.md` + `.workbuddy/key-customers-all.json` 逐条对应）⇒ 把我原先只标"法务/商务判断项"的 **中** 级问题**升级为高危商务决策**，需 human 定。
- **全稿零金额、零价格、零人天、零店数** ⇒ 我未涉足的"10 倍金额单位坑"风险**已闭合**，可从汇总中划掉。

**6.5 仍无定论的（本 Thread 内无人能出）**
**版式/读图轴**：`read_image` 对我与产品经理两人的模型**都**报 `does not declare image input` ⇒ 封面观感、配色档次、版式美观**本 Thread 无人能判**，**不要默认已有人接**，需 human 亲看或换一个能读图的模型。
**我这条轴能给该轴的最大贡献（不需要读图）**：封面当前**只有渐变底 + 文字 + 5 个能力词，没有任何产品画面**（3.2 已证：设备图函数是死代码）——这也是**唯一一处"改一个调用就能明显提升封面"**的低成本改法。

**6.6 第三轮（产品经理 与 leader 的复核）——P8 取值 `0.75`→`0.6`，以及"删 `bold` 不是一行"的定案**
- **P8 取值**：产品经理 指出 `h=0.75` 会让框下沿到 2.25in，与 `build-ppt.py:347` 第一条 bullet 标签框（起点 2.15in）**相叠 0.10in**；墨迹虽不撞，但**摆框派复核会凭空多报一条"文字互压"**。我据此重算 ⇒ **`h=0.6`：位图 86px、余量 ≥18px，下沿 2.10in < 2.15in，取值上直接消掉框叠**，不必依赖复核人守住墨迹口径。**本报告 2.1 与 Verdict blocker 2 已统一为 `0.6`。**
  · 口径差异（已闭合）：我像素实测**行距 36px**（需 ≈68px）；leader 按源码 `line_height = max(font_size+3, bbox+4)` 得 35px（需 ≈70px）——**两种模型都远小于 86.4px**，不再争 2px。
  · 改框高会使该位图 **50px → 86px** ⇒ 复核必须**按新画布高**判"是否触底"，不得拿今天的 row 49 当期望值（已记入 2.1）。
- **"删 `bold`"定案为"改名不删"**：我用 `ast` 静态扫出 **89 调用点 / 71 处传 `bold` / 25 处传 `align` / `bold=`与`font=` 关键字各 0 次**；leader 用 `ast` 独立扫出**同一组数字与同一份 25 行行号列表**。⇒ 见 3.5：`font` 可安全删（0 处受影响），`bold` 本轮**只改名 `_bold_unused`**，否则 25 处 `align` 会静默错位成左对齐。
- **防推翻的锁（产品经理）**：我 3.5 原措辞"所有 `bold=True` 的实参"会被 `grep -c 'bold=True'` = 0 反手推翻——因为传的是**位置实参**。已改写。
- **断言上界（leader）**：两条路都不改渲染 ⇒ 修后只断言"位图张数/尺寸集合不变"，**不做像素/字节断言**（重跑用当前 Pillow，编码未必一致）。

---

## 七、Verdict

# `改后发`

**必须先修的 blocker（2 条）**

1. **能力口径**：把 圈子 / 消息中心 / Push / 合规组件 / 后台配置化装修 / 行情 从「我们的能力」降级为「定制项 / 规划中」，并加统一页脚免责线。依据：`基础包范围与报价单-收窄版.md:20-31`、`app-delivery-notes/交付成本与工期边界.md:45-62`、`:183`。**这是唯一一条"不改就可能违约"的问题，优先级高于所有版式问题。**
   （最小改动版：不动叙事，只在第 5、8、9、10、13、14 页加「定制项」角标 + 全篇统一页脚。）
2. **第 8 页文字被切**（`build-ppt.py:346`，框高 `0.35`→**`0.6`**）——客户肉眼可见的破版，改一个数字即可。**取 0.6 而非 0.75**：0.6 既能装下 2 行（86px ≥ 68px），又不会与下方 bullet 框相叠；0.75 会引入 0.10in 框叠，逼迫复核改用墨迹口径（详见 2.1）。

**建议同批修（不阻塞发稿，但很便宜）**
- 假开关/死代码一组：`title()` 的 kicker（87-90 + 13 处调用）、**`add_cover_device()`(105) 与 `add_cover_gradient()`(150) 两个封面死函数**、**`text()` 的 `bold` 与 `font` 两个死形参（37 行）**——**先决定"要 kicker / 要封面样机图 / 要不要字重"再改**，这四条都是"作者想做但没生效"，属意图丢失；**但接上 Bold 会改字宽、折行位置全变，必须重跑裁切核验**，所以建议先按"删假开关"最小化处理。
- 封面页脚重复绘制（**删 239 行**）。
- 文档属性补 author/title（现在是 python-pptx 模板的 2013 年 + Steve Canny；`dc:title`/`dc:creator` 为空）。
- 截图旁加一句「原型示意，非已交付产品」（第 5/8/10 页），与第 12 页的「Demo 示意」口径拉齐。
- **页码右缘那条已降级为偏好、不列入返工**（见 2.4/6.2）。

**不建议因此返工**：正文 10pt / 页脚 8pt 的字号（偏好）；"无文字层"这一取舍本身（它是**可接受的生成方式**，问题在**对外交付形态**，见 1.3）。

**我这条轴的总判断**：叙事与信息架构是能打动人的，**问题不在"讲得好不好"，而在"讲的这些我们能不能交"**——稿子目前对我方自己的收窄口径**没有做任何降级或标注**，而这恰恰是我方文件里唯一被标为"违约风险"的红线。
